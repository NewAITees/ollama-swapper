# ollama-swapper
A lightweight swapper for Ollama models — auto unload, auto swap, zero VRAM waste.

## Features
- Model lifecycle control for Ollama
- Auto-unload idle models (keep_alive = 0 default)
- Proxy with model-specific context & keep_alive policies
- Sweep loaded models easily
